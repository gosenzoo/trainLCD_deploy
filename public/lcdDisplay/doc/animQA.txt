質問１回目
１．
public\jsMojules\utilClass\Animator.jsです。

２．
はい、SVGテンプレートの指定です。指定子はlcd-animType="kuru"/"fade"/"nothing"です。この属性がなければ、
"nothing"です。
kuruTop/Bottomもテンプレート指定で、指定子はlcd-kuruTop,lcd-kuruBottomです。この属性がなければ、それぞれ
テキストボックスの上下端を使ってください。

３．
はい、そうしてください。描画ツリーにつなげた状態でも、各オブジェクトは自分のDOMへの参照を持っているようにしてください。

４．
group全体にアニメーションを付けてください。

５．
再利用します。


質問（２回目）
６．
langChange()が起こった時点でdrawParamsが変わっている前提です。drawParamsを送ったのちlangChange()を実行する形になると思います。

７．
APIというより、参考にして新しい関数やクラスを実装してください。本環境のAnimator.jsは、アニメーションの無限ループを前提としており、
前提が異なります。

８．
次に発生アニメーションが表示の乱れなくすぐに出せる形であれば何でもよいです。お任せします。

９．
レイアウトは変えないでください。見た目の変化のみです。